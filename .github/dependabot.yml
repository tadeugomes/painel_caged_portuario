version: 2
updates:
  # Manter as dependências do GitHub Actions atualizadas
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 10

  # Manter as dependências do R atualizadas (via DESCRIPTION se existir)
  # Descomente as linhas abaixo se você tiver um arquivo DESCRIPTION
  # - package-ecosystem: "github-actions"
  #   directory: "/"
  #   schedule:
  #     interval: "monthly"
  #   open-pull-requests-limit: 10
